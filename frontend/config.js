// Point this to your deployed backend.
// While developing locally, keep it as http://localhost:5000
// After deploying the backend (Render/Railway/Fly.io), replace with that URL.
const VOLO_API_URL = "https://volo-backend.onrender.com/";
